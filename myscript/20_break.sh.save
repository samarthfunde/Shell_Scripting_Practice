#!/bin/bash

no=5
found=0   # flag

for i in 1 2 3 4 5 6 7 8
do
    if [[ $no -eq $i ]]
    then
        echo "$no is found"
        found=1   # mark true
        break
    fi
done

if [[ $found -eq 0 ]]
then
    echo "Number is Not Found"
fi

